interface:
  display_name: "角色 IP 工作室"
  short_description: "从参考图制作统一的角色素材套件"
  default_prompt: "Use $character-ip-studio to create a reusable character asset kit from an uploaded reference image."

policy:
  allow_implicit_invocation: true
